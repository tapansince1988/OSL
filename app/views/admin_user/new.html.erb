<%= form_for(:admin_user, :url => {:action => "create"}, :html => {:multipart => true}) do |user| %>
    <%= render(:partial => 'form', :locals => {:u => user}) %>
    <p class="text-center">
      <%= submit_tag("Create New") %>
    </p>
<% end %>