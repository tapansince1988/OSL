<%= form_for(:store, :url => {:action => "create"}, :html => {:multipart => true}) do |store| %>
    <%= render(:partial => 'form', :locals => {:s => store}) %>
    <p class="text-center">
      <%= submit_tag("Create New") %>
    </p>
<% end %>